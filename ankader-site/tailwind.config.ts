import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#14C3D0",
        secondary: "#0F2C41",
        background: "#F9F8F3",
        accent: "#738694",
      },
    },
  },
};

export default config;
